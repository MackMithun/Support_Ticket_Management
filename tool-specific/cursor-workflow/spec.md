# Spec

Build a support ticket workspace with create, list, search, filter, status update, and comment capabilities. Ensure the backend enforces valid transitions and the frontend presents the workflow clearly.