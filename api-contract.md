# API Contract

## Endpoint
GET /api/tickets

### Purpose
Retrieve tickets with optional search and status filtering.

## Endpoint
POST /api/tickets

### Purpose
Create a new ticket.

## Endpoint
PATCH /api/tickets/{id}/status

### Purpose
Move a ticket to the next valid state.
